# Configure your backend here (e.g. S3) for remote state
terraform {
  # backend "s3" {
  #   bucket         = "autoops-terraform-state"
  #   key            = "aws-sample-ecs-app/terraform.tfstate"
  #   region         = "ap-south-1"
  #   dynamodb_table = "autoops-terraform-locks"
  # }
}
