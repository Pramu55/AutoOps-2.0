import { IntegrationsHubClient } from './integrations-hub-client';

export const metadata = {
  title: 'Integrations Hub | AutoOps',
};

export default function IntegrationsHubPage() {
  return <IntegrationsHubClient />;
}
