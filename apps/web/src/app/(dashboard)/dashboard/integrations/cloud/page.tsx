import { CloudReadinessClient } from './cloud-readiness-client';

export const metadata = {
  title: 'Cloud Readiness | AutoOps',
};

export default function CloudReadinessPage() {
  return <CloudReadinessClient />;
}
